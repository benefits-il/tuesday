---
description: Make a multi-slide branded Instagram carousel
argument-hint: [the carousel content, e.g. "a carousel from carousel-outline.md"]
---

Make a branded carousel for this request:

$ARGUMENTS

Hand this to the visual-producer agent. Use the make-branded-carousel skill: read the brand sheet and the per-slide outline, fix one precise visual system, and produce every slide in the exact same look by calling the image connector once per slide. If no brand sheet is attached, ask for one.
